# 受け入れ基準の証跡（Phase 1）

確認日: 2026-09-03　データ版: 2026-09-03-0605（ChatGPT 実装レビュー2回目の修正反映後）　基準の定義: `実装指示書_Phase1.md` 第5節

| # | 基準 | 結果 | 証跡 |
|---|---|---|---|
| 1 | 対象 192 ファイル（単走 185 ＋ Q1/Q2 形式 7）が第0〜4層で緑。第5層は結果を明記 | **OK** | `python -m etl.build --fis` → `4413 records / 49 warnings / 0 errors / 公開対象 192/192 ラウンド`。第5層は 32 大会 × 男女 = 64 組すべてで FIS HTML と照合（期待 64 / 実行 64）。ラウンド状態は ok 188、upstream_missing 4（Almaty 2023-24 の決勝1 男女: 決勝2 の PDF が FIS 未公開）。実行件数: 2方式一致 4,413/4,413 ラン、再計算 4,131 OK ラン、順位再構成 192/192 ラウンド、正解 31、変異 12。`検証レポート.md` |
| 2 | 正解データ 20 本以上と A・B の出力が一致 | **OK** | `golden/golden_phase1.json` 31 記録（標準・複数ページ・RES・Q1/Q2 二段・DNF/DNS ブロック・ターン下限 0.3・NJ・結合 NOC・減点合計空白・氏名折り返し・ペースタイム "s" 付き）。PDF をピクセル画像に描画して目視で作成。ビルドで全一致 |
| 3 | 変異テスト 10 種以上で少なくとも1層が止める | **OK** | `python -m etl.tests.test_mutations` → 12 種すべて検出（J1/J2 入替、氏名入替、FIS コード1桁、ジャンプコード、DD、タイム、選手削除、ラン重複、F1 から削除、順位入替、減点符号、Q2 誤ラベル） |
| 4 | 3画面が PC 幅と 390px 幅で横スクロールなし、JS エラーなし | **OK** | 画面担当が http.server で確認（1280px / 390px、`scrollWidth == clientWidth`、コンソールエラー 0）。本人でも 3 画面の本文とコンソールを確認 |
| 5 | 選手ページの「上のラインとの差」が手計算と一致 | **OK** | 西沢岳人 2026-02-28 Nanto-Toyama F1: タイム点 17.22 − 16.63 = +0.59、合計 71.44 − 75.48 = −4.04（画面表示と一致）。画面担当も別例（18.58 − 17.51 = +1.07）を確認 |
| 6 | README に大会追加の手順（trampo-results と同じ書式） | **OK** | `README.md` 第1節 |
| 7 | push 前に Van さんが内容を確認する | **未** | 未 push。ローカルで `python -m http.server 8765` → http://localhost:8765/ で確認可能 |

## 補足

- 第1層（座標パーサ A と行テキストパーサ B の全項目一致）で見つかって直した A 側の不備: 氏名の2行折り返し、Tie/RES 列の座標帯、ハイフン付き姓と国コードの結合、Q1参照ブロックへの採用点・Tie の複製。
- 正解データで見つかった不備: 氏名折り返し（GERKEN SCHOFIELD Makayla 等）、Tie 帯。
- 第5層で見つかった欠落: Almaty 2023-24 決勝2（FIS 未公開）。`etl/known_gaps.json` に登録し画面に注記。
- 警告 49 件の内訳: 欠場・シード直接進出（Almaty 2023 W杯ファイナルは上位シードが予選免除）17、氏名・国の表記ゆれ 7、ラウンド間の審判構成違い 3（予選と決勝で codex が別の大会）、既知の欠落関連 22。いずれも人が見て判断する情報で、データ誤りではない。
- 未確認: GitHub Actions からの FIS 到達性（初回実行時に確認）。`athlete_aliases.json` の漢字は一部未確認（`kanji_unverified`）。

## ChatGPT 実装レビュー（2回目、2026-09-03）への対応

| 指摘 | 対応 |
|---|---|
| 修正必須1: FIS データの自動取得・公開リポジトリでの再配布可否を確認 | コードではなく公開条件。`update.yml` の定期実行をコメントアウト（手動実行のみ）。Van さんの判断事項として報告 |
| 修正必須2: 点の経済学は理論式でなく本番採点関数で計算 | `js/scoring.js` に FIS の採点関数（整数セント、切り捨て、上限）を実装し、タイム・エア・DD 損益分岐をすべて再計算方式に変更。全 4,131 OK ランで印字値と一致確認。ベース点の文言も「採点に入る3名がそれぞれ0.1上がると+0.30」に修正 |
| 修正必須3: 減点差の符号統一、「優勝者」→「同ラウンド1位」 | 値は元から「自分 − 相手」で統一されていた。行名を「減点による得点差」、列名を「同ラウンド1位」に変更し、凡例を追加 |
| 修正必須4: 「0件検証で緑」を禁止 | 第5層は照合できた大会数 = 期待数を条件に。0行・レース不明はエラー。第1層は全ラウンド比較済みを条件に。manifest に実行件数を記録し「データについて」に表示 |
| 推奨: 既知の欠落を3値化 | 第5層の状態を ok / upstream_missing / error に分け、画面では「△ 一部未収録（FIS 未公開）」と表示 |
| 推奨: 規則版の選択キーを日付だけにしない | 現状は季節版 × フォーマット（大会種別から決定）。26/27 の改定時に ruleset_id を確定値として持つ形へ拡張予定（Phase 2 課題） |
